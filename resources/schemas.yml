# 💡 Additional resource specifications can be found at https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/resources
# NOTE: If these have already been deployed via config/base_permissions.py or Terraform this will not work
# We default to Terraform for deployment of volumes. Defer to the client's preference of how resources should be deployed and coordinate with Network & Security.
#resources:
#  schemas:
#    # ⚠️ Note that GRANTs should be provided via Terraform
#    bronze:
#      catalog_name: ${var.catalog_name}
#      name: ${bundle.name}_bronze
#      comment: Raw, append-only data for the ${bundle.name} data product. External access is restricted in order to enforce data contract.
#    silver:
#      catalog_name: ${var.catalog_name}
#      name: ${bundle.name}_silver
#      comment: Transformed, intermediate data for the ${bundle.name} data product. External access is restricted in order to enforce data contract.
#    gold:
#      catalog_name: ${var.catalog_name}
#      name: ${bundle.name}
#      comment: Published tables for the ${bundle.name} data product.
#    quality:
#      catalog_name: ${var.catalog_name}
#      name: ${bundle.name}_quality
#      comment: Data quality schema for the ${bundle.name} data product. Used for Lakehouse Monitoring.